def send_email(to: str, subject: str, html: str) -> None:
    # Placeholder: plug Resend/Postmark SDK here.
    # Keep this interface stable so provider swap is one file change.
    _ = (to, subject, html)
