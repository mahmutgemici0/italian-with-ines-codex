import { Card, Container, SectionTitle } from "@/components/ui";

export default function AdminMerchPage() {
  return (
    <Container><section className="py-16 space-y-3"><SectionTitle title="Manage Merch" /><Card>Manual merch list now; adapter layer ready for Printful/Printify sync later.</Card></section></Container>
  );
}
